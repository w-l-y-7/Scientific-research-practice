# Zotero 检索结果

> 从本地 Zotero 库搜出来的条目，贴到这里。用 `mcp__zotero__zotero_search_items` 搜，
> 命中的条目用 `mcp__zotero__zotero_item_metadata` 补全字段。
>
> 这是**原始检索记录**，不在这里筛。筛完的结果放 `candidate_papers.md`。

## 怎么用

一个检索式一节。**检索式和检索日期必须留**——一篇说不清自己检索过程的综述，
别人没法复现，也没法判断你漏没漏。

命中的条目先原样记下来，哪怕看着不相关。这时候判断相关性还太早。

## 状态

未跑。

## 检索记录

### <检索式>

- 检索日期：
- 命中条数：
- 检索范围：

| 标题 | 第一作者 | 年份 | Zotero key | 备注 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |
