# 外部数据库检索结果

> Zotero 之外的库搜出来的条目。检索走 `paper-lookup` 技能（18 个学术 API）。
> 各库的检索语法和限流规则见 `.claude/skills/paper-lookup/references/<库名>.md`，**搜之前先读**。
>
> 和 `zotero_results.md` 一样，这是原始记录，不在这里筛。

## 怎么用

按库分节，一个检索式一条记录。**至少查 3 个库**——只查一个必漏文献。

金融科技方向起步用这四个：

| 库 | 为什么 |
| --- | --- |
| Semantic Scholar | 覆盖面广，能按引用数排序，让有影响力的先冒出来 |
| OpenAlex | 元数据全，作者消歧做得好 |
| arXiv（`econ.GN` / `econ.EM`） | 预印本更新快，正式发表要等一两年 |
| Crossref | DOI 和正式发表记录以此为准 |

## 状态

未跑。

## 检索记录

### <库名>｜<检索式>

- 检索日期：
- 命中条数：
- 排序方式：
- 导出文件：`sources/<文件名>`

| 标题 | 第一作者 | 年份 | DOI | 来源库 | 备注 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |
