# 文献综述（初稿）

> 由 `review-writer` 代理生成，按主题组织，每个主题一个二级标题。
> 输入是 `drafts/outline.md` + `literature_notes/summary_table.md` + `literature_notes/grade_assessment.md`。

## 状态

未生成。三个前置依赖没一个就位：

| 依赖 | 现状 |
| --- | --- |
| `drafts/outline.md` | 有文件，但主题还是「待定」 |
| `literature_notes/summary_table.md` | 空表，没有文献 |
| `literature_notes/grade_assessment.md` | 未跑 |

大纲本身就写着「主题从文献里长出来，先把 summary_table 读完再定」。表是空的，
所以现在调 `review-writer` 要么拒绝、要么编——两种都不是你要的。

**注意**：重跑 `review-writer` 会整个覆盖本文件。要保留手改的段落，先另存一份。

## 正文

（跑完 `review-writer` 后写在这里）
